这是最核心的转变。不要告诉 Claude “怎么做”，而是告诉它“要什么”。
指令型 Prompt（低效）：“打开 order.ts，找到 createOrder 函数，在第15行加一个 if 判断，检查 amount 是否大于0...”
问题：你的思考限制了 Claude 的能力，它不会帮你处理没想到的边界情况。
目标型 Prompt（高效）：“确保 createOrder 函数能正确处理非法金额的情况，包括负数、零、超大数值和非数字输入，按项目现有的错误处理规范返回。”
优势：Claude 会主动考虑你没想到的边界情况（如溢出、精度问题），效果可能提升数倍。

写好提示词是高效协作的基础。
结构化提示词：一个高质量的 Prompt 应包含三个要素：
上下文 (Context)：明确文件和位置。例如，不要说“修复认证bug”，要说“在 lib/auth.ts 中，validateToken() 函数没有处理 JWT 过期的情况”。
期望动作 (Action)：使用明确的动词，如“添加”、“替换”、“删除”或“重构”。
成功标准 (Success Criterion)：明确验收条件，如“现有测试必须通过”。
善用文件引用 (@)：在聊天中输入 @ 并开始输入文件名，Claude Code 会触发自动补全。被引用的文件内容会直接注入到上下文中，让 Claude 快速理解。
先计划，后执行：
口头要求：在复杂改动前，直接要求 Claude：“先解释你的计划，再修改代码”。
Plan Mode：按 Shift+Tab（或 Alt+M）切换到计划模式。在此模式下，Claude 会先提出实施计划，确认后再动手。这能比直接编码多检测出 3 倍的边缘情况。

 会话与上下文管理：保持专注与高效
单次会话，单一目标：将每次对话限制在一个明确的目标上（如：一个bug、一个功能）。聚焦的会话生成的代码被采纳的概率能高出 73%。
善用开场陈述 (Framing Statement)：在会话开始时，清晰说明项目背景，能显著提升后续回答的相关性。
例如：“你正在一个 Next.js 15 / TypeScript / Prisma 的项目上工作，入口文件是 app/page.tsx。”
管理上下文窗口：
/clear：当对话偏离主题或 Claude 混淆文件时，运行 /clear 可以清空上下文，开启新会话。
/compact：当上下文窗口快满时，运行此命令让 Claude 压缩对话历史。
/statusline：运行此命令可在终端底部开启一个持久的状态栏。你可以自定义显示当前分支、上下文窗口使用百分比等信息，一目了然。

 实用命令与快捷键
执行 Shell 命令 (!)：在聊天中直接用 ! 前缀运行 Shell 命令。命令输出会直接成为对话的一部分，供 Claude 分析。例如，可以让 Claude 运行测试并直接根据输出进行调试。
小技巧：在 ! 命令运行时按 Ctrl+B 可将其转入后台运行。
/cost：随时运行此命令查看当前会话的 Token 消耗和费用。
/rewind (按两次 Esc)：这是一个强大的“后悔药”。你可以选择：
仅恢复代码：保留对话，但撤销代码改动
仅恢复对话：保留代码改动，但清除部分对话历史以释放上下文空间。
全部恢复：同时撤销代码改动和对话历史。



///////////////////////////////////codex////////////////////////////////////////////

核心工作流：从“改代码”到“完成任务”
高效的 Codex 工作流，应该是一个“计划-执行-验证”的闭环。核心是先对齐思路，再让它动手。
先用 /plan 制定计划：对于任何跨文件或不明确的任务，首先使用 /plan 命令。让 Codex 先提供一个清晰的执行方案，而不是直接修改代码。
提示词示例：“先不要改代码。请先读相关文件，给我一个计划：1. 要改哪些文件。2. 哪些地方不碰。3. 改完怎么验证。4. 这件事最容易出问题的点是什么。”
再用 /goal 设定目标：对于重构、迁移等长期任务，使用 /goal 命令。设定一个明确的“终点线”（完成标准），防止 Codex 在长跑中偏离方向。
提示词示例：“目标：把这个模块从旧接口迁移到新接口。完成标准：- 相关单测全部通过。 - 页面原有行为不变。 - 不改认证逻辑。”
最后用 /permissions 管理权限：通过 /permissions 命令精细控制 Codex 的修改权限。既可以避免频繁中断，也能防止改动范围无节制扩大，让审查 Diff 更轻松。

 提示词技巧：让 Codex 更懂你
好的提示词是高效协作的基础。
使用“GOCC”模型：这是一种结构化的提示词方法。
Goal（目标）：明确要实现什么功能。
Origin（来源）：指定相关的文件或文档。
Constraints（约束）：定义技术规范和安全要求。
Criteria（标准）：说明验收条件。
示例：// 目标：为订单服务添加库存校验逻辑 // 来源：@services/order.js @models/inventory.js // 约束：使用乐观锁，超时3秒 // 标准：通过单元测试
善用“四维定位法”构建上下文：通过指定 目标、上下文（相关文件）、约束和完成条件，可以极大提升代码生成的准确率。
提示词示例：“实现一个用户权限校验中间件。上下文：@src/middleware/auth.js, @docs/api-design.md。约束：必须使用JWT验证。完成条件：通过现有测试套件。”
用语音输入捕捉灵感：当你思路还不清晰时，直接用语音说出你模糊的想法。这能帮你保留原始的、跳跃的思考，而不必纠结于如何组织完美的书面提示词。


////////////////////0630///////////////////////
su_mq/kafka_mq.go添加读取kafka代码，多协程读，注意并发bug